<div class="hero text-center w-full h-svh flex items-center px-2">
  <!--   <div class="h-24 bg-gradient-to-t from-pinkown w-full absolute self-end"></div> -->
  <div class="w-screen-2xl mx-auto">
    <h2
      class="font-normal xl:tracking-wide text- animate-fade-up animate-duration-1000 text-5xl md:text-6xl"
    >
      15 años creciendo contigo
    </h2>
    <div
      class="invitacion animate-fade animate-duration-1000 animate-delay-500 w-full hover:scale-105 transition-all bg-amber-100/60 backdrop-blur-md shadow-2xl border-separate border-4 mt-10 mx-auto"
    >
      <header
        class="p-2 border-b border-dashed border-slate-900 max-w-screen-sm mx-auto"
      >
        <h3 class="mt-2 mb-6">
          <span
            class="text-transparent bg-clip-text bg-gradient-to-r to-violet-700 from-pink-600"
            >¡Gracias por acompañarnos!</span
          >
        </h3>
        <h4 class="text-xl mx-auto">
          Celebramos nuestros primeros quince y agradecemos a toda la comunidad
          CCH por hacer de este proyecto una realidad.
        </h4>
        <span class="mt-5 block"
          >Seguiremos siendo ese espacio de confianza donde encontrarán recursos
          digitales de calidad, creados con mucho amor y dedicación por nuestros
          docentes.</span
        >
        <span class="mt-5 block">
          Nuestro objetivo es seguir siendo un referente en la oferta de
          recursos digitales de libre acceso e invitamos a todos los docentes a
          seguir compartiendo sus materiales y a los estudiantes a aprovechar al
          máximo estos recursos.
        </span>
      </header>
      <footer class="max-w-screen-sm">
        <h4 class="p-5">
          <span
            class="text-transparent bg-clip-text bg-gradient-to-r to-violet-700 from-pink-600 text-xl"
            >¡Gracias por ser parte de esta gran comunidad!</span
          >
        </h4>
      </footer>
    </div>
  </div>
  <style>
    h2 {
      font-family: var(--font-titulos);
    }
    h3 {
      font-size: var(--headings-lg);
      font-family: var(--font-cuerpo);
    }
    .invitacion {
      h3 {
        font-size: var(--headings-xl);
        font-family: var(--font-titulos);
      }
    }
    #counter-back {
      font-family: var(--font-cuerpo);
    }
    .hero {
      background-image: url("/hero/hero-portada-7.webp");
      background-size: cover;
      background-repeat: no-repeat;
      background-position: bottom center;
      background-attachment: fixed;
    }
  </style>
  <script is:inline>
    const counterBack = document.getElementById("counter-back");
    const targetDate = new Date("November 21, 2024 10:00:00").getTime();
    const targetDateElement = document.getElementById("target-date");
    const targetTimeElement = document.getElementById("target-time");

    function updateCounter() {
      const now = new Date().getTime();
      const distance = targetDate - now;

      const days = Math.floor(distance / (1000 * 60 * 60 * 24));
      const hours = Math.floor(
        (distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60)
      );
      const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((distance % (1000 * 60)) / 1000);

      counterBack.innerHTML = `${days} <span class="font-bold">días</span> ${hours} <span class="font-bold">horas</span> ${minutes} <span class="font-bold">minutos</span> ${seconds} <span class="font-bold">segundos</span>`;

      if (distance < 0) {
        clearInterval(interval);
        counterBack.innerHTML = "¡Llegó el día felices 15 años!";
      }
    }

    const interval = setInterval(updateCounter, 1000);
    updateCounter();
    const options = { year: "numeric", month: "long", day: "numeric" };
    targetDateElement.innerHTML = new Date(targetDate).toLocaleDateString(
      "es-ES",
      options
    );

    const timeOptions = { hour: "2-digit", minute: "2-digit" };
    targetTimeElement.innerHTML =
      new Date(targetDate).toLocaleTimeString("es-ES", timeOptions) + " hrs.";
  </script>
</div>
